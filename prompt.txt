        prompt = f"""You are an intent classifier for a library system.
Current user: {self.current_user_name}

Output ONLY a JSON object: {{"intent": "INTENT", "params": {{...}}}}

🚨 CRITICAL RULE 🚨: You MUST default to "chat" for all greetings, insults, random questions, or general conversation. ONLY use the other intents if the user EXPLICITLY asks to perform a library action.

INTENTS:
- "library_stats" - How many books does the LIBRARY have
  Examples: "how many books do you have", "total books"
- "who_am_i" - User asks about THEIR identity
- "my_books": {{"intent": "my_books", "params": {{}}}}
  Examples: "what books did I borrow", "my checkouts"
- "return_book": {{"intent": "return_book", "params": {{"title": "ONLY the book title, or 'unknown' if not mentioned"}}}}
- "borrow_book" - Borrow a book. params: {{"title": "book title", "quantity": number}}
- "search_books" - Find books by subject. params: {{"query": "keyword"}}
  Examples: "books about science" → "science", "dystopian novels" → "dystopian"
- "browse_books" - See what library has
- "register" - Register. params: {{"name": "full name", "user_id": "ID", "role": "student/faculty"}}
- "chat" - Greetings, questions about suggested books, "what can you do", anything conversational

CRITICAL: "how many books do YOU have" = library_stats. "how many books do I have" = my_books.
1. "What is YOUR name" = chat. 
2. "What is MY name" = who_am_i.
3. "What books do I like" = my_preferences.
User: {user_input}
JSON:"""

